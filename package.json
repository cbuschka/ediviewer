{
  "name": "ediviewer",
  "version": "0.1.0",
  "private": false,
  "homepage": "https://cbuschka.github.io/ediviewer",
  "engines": {
    "npm": ">=6.0.0",
    "node": ">=14.17.0"
  },
  "dependencies": {
    "@cbuschka/flux": "^4.0.0",
    "@testing-library/jest-dom": "^5.16.2",
    "@testing-library/react": "^12.1.2",
    "@testing-library/user-event": "^13.5.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-dropzone": "^12.0.2",
    "react-scripts": "5.0.0"
  },
  "scripts": {
    "start": "REACT_APP_GIT_COMMITISH=`git rev-parse --short HEAD --always --dirty` REACT_APP_BUILD_TIMESTAMP=`date --rfc-3339=seconds` react-scripts start",
    "build": "REACT_APP_GIT_COMMITISH=`git rev-parse --short HEAD --always --dirty` REACT_APP_BUILD_TIMESTAMP=`date --rfc-3339=seconds` react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "jest": "^27.5.1"
  }
}
